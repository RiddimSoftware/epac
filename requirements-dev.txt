# Development dependencies for local tooling and CI scripts.
# Install with: pip install -r requirements-dev.txt

# Cucumber's reference Gherkin parser — used by scripts/journeys/lint.py
gherkin-official==39.0.0

# Test runner for repo-local Python CI helper tests.
pytest==9.0.3

# YAML parser for GitHub Actions workflow contract tests.
PyYAML==6.0.3
