name: pr-build

on:
  pull_request:
    branches: [main]
  merge_group:

permissions:
  contents: read

jobs:
  pr-build:
    uses: RiddimSoftware/riddim-release/.github/workflows/pr-build.yml@main
    with:
      scheme: "epac"
      project: "epac.xcodeproj"
      working_directory: "ios"
      runner_labels: "${{ vars.RUNNER_LABELS_MAC || '[\"self-hosted\",\"macOS\"]' }}"
