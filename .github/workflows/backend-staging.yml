name: Backend Staging Deploy

on:
  push:
    branches: [main]
    paths:
      - 'backend/**'
      - '.github/workflows/backend-staging.yml'
      - 'scripts/ci/backend_staging_smoke.py'
  workflow_dispatch:

concurrency:
  group: backend-staging-deploy
  cancel-in-progress: false

permissions:
  contents: read
  id-token: write

jobs:
  deploy:
    name: Deploy ${{ matrix.service }} to staging
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        service: [daily-fetch, loader, search, members, sittings, bills, member-speeches, member-votes, on-this-day, riding-boundary, health, device-register, openapi, live-status]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: backend/${{ matrix.service }}/go.mod

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_BACKEND_STAGING_ROLE_ARN }}
          aws-region: us-east-1

      - name: Test and package Lambda
        working-directory: backend
        run: make test package SERVICE=${{ matrix.service }}

      - name: Update staging Lambda
        working-directory: backend/${{ matrix.service }}
        env:
          SERVICE: ${{ matrix.service }}
          FUNCTION_NAME: epac-${{ matrix.service }}-staging
        run: |
          set -euo pipefail

          if ! aws lambda get-function --function-name "$FUNCTION_NAME" >/dev/null 2>&1; then
            if [[ " members sittings bills " == *" $SERVICE "* ]]; then
              echo "$FUNCTION_NAME does not exist yet; apply infra/terraform/staging before deploying this new artifact-backed Lambda."
              exit 0
            fi
          fi

          aws lambda update-function-code \
            --function-name "$FUNCTION_NAME" \
            --zip-file fileb://function.zip
          aws lambda wait function-updated \
            --function-name "$FUNCTION_NAME"

      - name: Remove staging legacy DATABASE_URL
        env:
          SERVICE: ${{ matrix.service }}
          FUNCTION_NAME: epac-${{ matrix.service }}-staging
        run: |
          set -euo pipefail

          if ! aws lambda get-function --function-name "$FUNCTION_NAME" >/dev/null 2>&1; then
            if [[ " members sittings bills " == *" $SERVICE "* ]]; then
              echo "$FUNCTION_NAME does not exist yet; skipping legacy DATABASE_URL removal until Terraform creates it."
              exit 0
            fi
          fi

          CURRENT_ENV=$(aws lambda get-function-configuration \
            --function-name "$FUNCTION_NAME" \
            --query 'Environment.Variables' \
            --output json)
          UPDATED_ENV=$(jq -cn \
            --argjson current "${CURRENT_ENV:-null}" \
            '{Variables: (($current // {}) | del(.DATABASE_URL))}')

          aws lambda update-function-configuration \
            --function-name "$FUNCTION_NAME" \
            --environment "$UPDATED_ENV" \
            >/dev/null
          aws lambda wait function-updated \
            --function-name "$FUNCTION_NAME"

      - name: Sync staging artifact environment
        if: ${{ contains(fromJSON('["members","sittings","bills","member-speeches","member-votes"]'), matrix.service) }}
        env:
          FUNCTION_NAME: epac-${{ matrix.service }}-staging
          ARTIFACT_BUCKET: ${{ vars.EPAC_ARTIFACT_BUCKET_STAGING || vars.EPAC_ARTIFACT_BUCKET || vars.ARTIFACTS_BUCKET }}
          ARTIFACT_PREFIX: ${{ vars.EPAC_ARTIFACT_PREFIX_STAGING || vars.EPAC_ARTIFACT_PREFIX }}
          SENTRY_DSN: ${{ secrets.SENTRY_DSN_STAGING }}
        run: |
          set -euo pipefail

          if ! aws lambda get-function --function-name "$FUNCTION_NAME" >/dev/null 2>&1; then
            echo "$FUNCTION_NAME does not exist yet; skipping artifact environment sync until Terraform creates it."
            exit 0
          fi

          if [ -z "$ARTIFACT_BUCKET" ]; then
            echo "EPAC_ARTIFACT_BUCKET_STAGING, EPAC_ARTIFACT_BUCKET, or ARTIFACTS_BUCKET variable is required"
            exit 1
          fi

          CURRENT_ENV=$(aws lambda get-function-configuration \
            --function-name "$FUNCTION_NAME" \
            --query 'Environment.Variables' \
            --output json)
          UPDATED_ENV=$(jq -cn \
            --argjson current "${CURRENT_ENV:-null}" \
            --arg artifact_bucket "$ARTIFACT_BUCKET" \
            --arg artifact_prefix "$ARTIFACT_PREFIX" \
            --arg sentry_dsn "$SENTRY_DSN" \
            '{Variables: ((($current // {}) | del(.DATABASE_URL)) + {EPAC_ARTIFACT_BUCKET: $artifact_bucket} + (if $artifact_prefix != "" then {EPAC_ARTIFACT_PREFIX: $artifact_prefix} else {} end) + (if $sentry_dsn != "" then {SENTRY_DSN: $sentry_dsn} else {} end))}')

          aws lambda update-function-configuration \
            --function-name "$FUNCTION_NAME" \
            --environment "$UPDATED_ENV" \
            >/dev/null
          aws lambda wait function-updated \
            --function-name "$FUNCTION_NAME"

  smoke:
    name: Smoke test staging backend
    runs-on: ubuntu-latest
    needs: deploy
    env:
      STAGING_API_BASE_URL: ${{ vars.STAGING_API_BASE_URL || 'https://staging-api.epac.riddimsoftware.com' }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_BACKEND_STAGING_ROLE_ARN }}
          aws-region: us-east-1

      - name: Ensure staging routes are synced
        env:
          ENV_NAME: staging
          STAGE_NAME: staging
        run: ./scripts/ci/ensure-staging-api-routes.sh

      - name: Run staging backend smoke tests
        run: python3 scripts/ci/backend_staging_smoke.py --base-url "$STAGING_API_BASE_URL"
